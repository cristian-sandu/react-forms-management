import { defineMessages } from 'react-intl';

const context = 'containers.Admin.Dashboard.components.EntityById';

export default defineMessages({
  ENTITY_BY_ID: {
    id: `${context}.ENTITY_BY_ID`,
    defaultMessage: 'Search Entity by Id',
  },
});
