import { defineMessages } from 'react-intl';

const context = 'containers.Admin.Dashboard.components.EntityByName';

export default defineMessages({
  ENTITY_BY_NAME: {
    id: `${context}.ENTITY_BY_NAME`,
    defaultMessage: 'Search Entity by Name',
  },
});
