import { defineMessages } from 'react-intl';

const context = 'containers.Admin.Dashboard.components.AssociationByName';

export default defineMessages({
  ASSOCIATION_BY_NAME: {
    id: `${context}.ASSOCIATION_BY_NAME`,
    defaultMessage: 'Search Association by Name',
  },
});
